{
    "name": "Brightness",
    "description": "Change the brightness of the image by given value",
    "inputs": {
        "brightness": {
            "type": "integer",
            "desc": "% brightness for the new image",
            "default": 0 
        }
    } 
}